server:
  port: 8080

spring:
  application:
    name: attachment-service
  profiles:
    active: dev
  config:
    import: "optional:configserver:http://localhost:8888"
  cloud:
    config:
      uri: http://localhost:8888

aws:
  s3:
    bucket-name: prolance-attachment
