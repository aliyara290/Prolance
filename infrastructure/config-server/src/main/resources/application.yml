server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/aliyara290/Prolance-Config-Repo
          search-paths: '{application}'
  application:
    name: config-server